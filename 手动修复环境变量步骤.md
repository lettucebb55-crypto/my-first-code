# 手动修复环境变量步骤（解决 Edge WebView2 安装问题）

## 问题原因

你的系统环境变量中，**用户级别**和**系统级别**的 `TEMP` 和 `TMP` 都设置为 `D:\Temp`，这可能导致安装程序无法创建临时目录。

## 解决方案（推荐：删除环境变量）

### 方法1：使用修复脚本（最简单）

1. **以管理员身份打开 PowerShell**
2. **运行修复脚本**：
```powershell
cd D:\python_code\Dj\baoding_tourism
.\修复环境变量并安装.ps1
```
3. **选择选项 1**（删除环境变量，使用系统默认）
4. **关闭所有 PowerShell 窗口**
5. **重新以管理员身份打开 PowerShell**
6. **运行安装程序**：
```powershell
& "D:\MicrosoftEdgeWebView2RuntimeInstallerX64.exe"
```

### 方法2：手动修改环境变量

#### 步骤1：删除用户级别环境变量

1. 按 `Win + R`，输入 `sysdm.cpl`，回车
2. 点击"高级"选项卡
3. 点击"环境变量"按钮
4. 在"Administrator 的用户变量"区域：
   - 选中 `TEMP`，点击"删除"
   - 选中 `TMP`，点击"删除"
5. 点击"确定"

#### 步骤2：删除系统级别环境变量

1. 在同一个"环境变量"窗口中
2. 在"系统变量"区域：
   - 找到 `TEMP`，选中后点击"删除"（如果存在）
   - 找到 `TMP`，选中后点击"删除"（如果存在）
3. 点击"确定"
4. 点击"确定"关闭所有窗口

#### 步骤3：重启 PowerShell 并安装

1. **关闭所有 PowerShell 和命令行窗口**
2. **重新以管理员身份打开 PowerShell**
3. **验证环境变量**：
```powershell
$env:TEMP
$env:TMP
```
应该显示类似：`C:\Users\Administrator\AppData\Local\Temp`

4. **运行安装程序**：
```powershell
& "D:\MicrosoftEdgeWebView2RuntimeInstallerX64.exe"
```

## 为什么删除环境变量？

- Windows 系统有默认的临时目录：`C:\Users\你的用户名\AppData\Local\Temp`
- 这个默认目录权限正确，所有程序都能正常使用
- 自定义的 `D:\Temp` 可能在某些情况下权限不足
- 删除环境变量后，系统会自动使用默认路径

## 如果不想删除，可以修改为系统默认路径

### 修改用户级别环境变量

1. 打开环境变量窗口（同上）
2. 在"用户变量"中：
   - 选中 `TEMP`，点击"编辑"
   - 将值改为：`%USERPROFILE%\AppData\Local\Temp`
   - 选中 `TMP`，点击"编辑"
   - 将值改为：`%USERPROFILE%\AppData\Local\Temp`
3. 点击"确定"

### 修改系统级别环境变量

1. 在"系统变量"中：
   - 找到 `TEMP`（如果存在），点击"编辑"
   - 将值改为：`%USERPROFILE%\AppData\Local\Temp`
   - 找到 `TMP`（如果存在），点击"编辑"
   - 将值改为：`%USERPROFILE%\AppData\Local\Temp`
2. 点击"确定"

## 验证修复

修复后，在新的 PowerShell 窗口中执行：

```powershell
# 检查环境变量
$env:TEMP
$env:TMP

# 应该显示类似：
# C:\Users\Administrator\AppData\Local\Temp
```

如果显示的是 `D:\Temp`，说明环境变量还没有生效，需要：
1. 关闭所有 PowerShell 窗口
2. 重新打开
3. 或者重启电脑（最彻底）

## 安装程序

环境变量修复后，运行：

```powershell
& "D:\MicrosoftEdgeWebView2RuntimeInstallerX64.exe"
```

应该不会再出现"无法创建临时目录"的错误了。




